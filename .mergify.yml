queue_rules:
  - name: default
    merge_conditions: [check-success=ci/never-runs]
    merge_method: merge
pull_request_rules:
  - name: echo-title-botaccount
    conditions: [-closed]
    actions:
      comment:
        message: "{{ title }}"
        bot_account: hugbunt3r1
